name: Release

on:
  release:
    types: [published]

jobs:

  Bump:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6
      - name: TODO
        run: echo TODO
